{
  "name": "quiet-quest",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "type": "module",
  "license": "",
  "author": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rimraf ./dist/js && concurrently \"tsc --watch -p ./src/ts/tsconfig.json\" \"nodemon\"",
    "build": "rimraf ./dist && tsc -p ./src/tsconfig.json && tsc -p ./src/ts/tsconfig.json",
    "start": "node ./dist/src/app.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/express-session": "^1.18.1",
    "@types/node": "^22.15.2",
    "concurrently": "^9.1.2",
    "dotenv": "^16.5.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.3",
    "typescript": "^5.2.2"
  }
}

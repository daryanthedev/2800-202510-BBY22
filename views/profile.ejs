<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('partials/header') %>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= user.username %> — Profile</title>
    </head>

    <body class="bg-[#BCDA94] min-h-screen flex flex-col">
        <!-- Header with curved bottom -->
        <header class="bg-gradient-to-r from-[#8FC14C] to-[#75A238] h-40 rounded-b-3xl flex items-end justify-center pb-6">
            <!-- Avatar -->
            <div class="w-28 h-28 bg-[#F7F1E6] rounded-full p-1">
                <img
                    src="<%= user.avatarUrl && user.avatarUrl.trim().length ? user.avatarUrl : '/blank-pfp.webp' %>"
                    alt="<%= user.username %>'s avatar"
                    class="w-full h-full rounded-full object-cover"
                />-[#F7F1E6] rounded-full p-1">
                <img
                    src="<%= user.avatarUrl && user.avatarUrl !== '' ? user.avatarUrl : '/assets/blank-pfp.webp' %>"
                    alt="<%= user.username %>'s avatar"
                    class="w-full h-full rounded-full object-cover"
                />" alt="<%= user.username %>'s avatar" class="w-full h-full rounded-full object-cover" />
            </div>
        </header>

        <!-- Main content card -->
        <!-- To adjust how much header is shown, update the negative margin: change '-mt-10' to a smaller value like '-mt-8' or '-mt-6' -->
        <main class="flex-1 bg-[#F7F1E6] -mt-5 rounded-t-3x p-6">
            <!-- Username -->
            <h1 class="text-3xl font-bold text-[#102128] text-center"><%= user.username %></h1>

            <!-- Bio Section -->
            <section class="mt-4">
                <h2 class="text-xl font-medium text-[#426F8A] mb-2 text-center">Bio</h2>
                <p class="text-[#102128] text-center text-lg">Bio goes here</p>
            </section>

            <!-- Placeholder Actions -->
            <section class="mt-6 space-y-4">
                <button class="w-full flex justify-between items-center px-6 py-4 bg-white rounded-lg shadow">
                    <span class="text-[#102128] text-lg">Placeholder Action 1</span>
                    <span class="text-[#426F8A] text-xl">→</span>
                </button>
                <button class="w-full flex justify-between items-center px-6 py-4 bg-white rounded-lg shadow">
                    <span class="text-[#102128] text-lg">Placeholder Action 2</span>
                    <span class="text-[#426F8A] text-xl">→</span>
                </button>
                <button class="w-full flex justify-between items-center px-6 py-4 bg-white rounded-lg shadow">
                    <span class="text-[#102128] text-lg">Placeholder Action 3</span>
                    <span class="text-[#426F8A] text-xl">→</span>
                </button>
                <button class="w-full flex justify-between items-center px-6 py-4 bg-white rounded-lg shadow">
                    <span class="text-[#102128] text-lg">Placeholder Action 4</span>
                    <span class="text-[#426F8A] text-xl">→</span>
                </button>
            </section>
        </main>
    </body>
</html>

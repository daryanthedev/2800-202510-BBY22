<!doctype html>
<html lang="en">
    <head>
        <%- include('partials/header') %>
        <title>About Quiet Quest</title>
    </head>
    <body>
        <div class="min-h-screen flex flex-col items-center justify-center bg-gray-100">
            <div class="bg-white rounded-lg shadow-lg p-8 flex flex-col items-center">
                <span class="text-6xl font-extrabold text-red-500 mb-4"><%= errorCode %></span>
                <h1 class="text-2xl font-semibold text-gray-800 mb-2"><%= errorName %></h1>
                <% if (typeof errorMessage !== "undefined") { %>
                    <p class="text-gray-600 mb-6"><%= errorMessage %></p>
                <% } %>
                <a href="/" class="inline-block px-6 py-2 my-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">Go Home</a>
            </div>
        </div>
    </body>
</html>
